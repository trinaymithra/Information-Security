def inverse_of_n (num,n) :
#Multiplicative inverse of a number in Zn

	for i in range(n+1) :
		if i*num % n == 1 :
			print(i)
			break

num = int(input("Enter the number for which inverse has to be calculated: "))
n=int(input("Enter 'n': "))
inverse_of_n(num, n)
